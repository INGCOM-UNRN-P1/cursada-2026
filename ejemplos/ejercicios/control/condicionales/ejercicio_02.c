#include <stdio.h>

int main(void)
{
    int es_admin = 0;

    if (es_admin = 1)
    {
        printf("Acceso concedido al panel de administracion.\n");
    }
    else
    {
        printf("Acceso denegado.\n");
    }
    printf("Estado final de es_admin: %d\n", es_admin);
    return 0;
}
